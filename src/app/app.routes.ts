import { Routes } from '@angular/router';
import { DynamicScreenComponent } from './dynamic-screen-component/dynamic-screen-component';

export const routes: Routes = [
    {path: '', component: DynamicScreenComponent}
];
